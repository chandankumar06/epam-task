import { IssuetitlefilterPipe } from './issuetitlefilter.pipe';

describe('IssuetitlefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new IssuetitlefilterPipe();
    expect(pipe).toBeTruthy();
  });
});

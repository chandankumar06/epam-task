import { ProducttitlefilterPipe } from './producttitlefilter.pipe';

describe('ProducttitlefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProducttitlefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
